#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints its name
 * @argc: number of argumts
 * @argv: array of arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}


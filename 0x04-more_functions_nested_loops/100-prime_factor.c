#include <stdio.h>

/**
 * main - find largest factor of 612852475143
 * Return: 0
 */

int main(void)
{
	long n, i, r;

	for (i = 0; i < n; i++)
	{
		if (n % i == 0)
		{
			r = n / i;
		}
	}
	printf("%lu\n", r);
	return (0);
}


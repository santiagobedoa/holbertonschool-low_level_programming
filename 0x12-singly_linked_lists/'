#include "lists.h"

/**
 *
 */
size_t print_list(const list_t *h)
{
	int len_linked_list = 0;

	if (h->str == NULL)
	{
		prinf("[0] (nil)\n");
		h = h->next;
	}
	while (h != NULL)
	{
		printf("[%d] %s\n", h->len, h->str);
		h = h->next;
		len_linked_list++;
	}
	return (len_linked_list);
}

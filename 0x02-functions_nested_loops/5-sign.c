#include <stdio.h>

/**
 * main - init
 * Return: 1 if greater than 0, 0 otherwise
 */

int print_sign(int n)
{
	if (n >) {
		printf("+, 1");
	} else if ( n == 0) {
		printf("0, 0");
	} else {
		printf("-, -1);
	}
}

